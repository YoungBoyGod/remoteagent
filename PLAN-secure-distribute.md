# SecureRelease 文件分发系统 — 实施规划

## 一、项目概述

基于现有 `分发.html` 原型，构建企业级 GPG 加密补丁分发系统。

### 精简策略（对比原型）

| 模块 | 处理方式 |
|------|----------|
| Header（标题 + 安全徽章） | **保留** |
| Progress Steps（4 步进度条） | **保留** |
| Stage 1: 选择 Release 包 | **保留** |
| Stage 2: 加密与完整性校验 | **保留** |
| Stage 3: 安全存储与链接生成 | **保留** |
| Stage 4: 客户分发配置 | **保留** |
| Success State（分发成功） | **保留** |
| 安全状态监控（右侧面板） | **删除** |
| 方案优势（右侧面板） | **删除** |
| 方案对比（右侧面板） | **删除** |
| 最近分发记录（右侧面板） | **保留，升级为完整分发记录页** |
| Security Report Modal | **保留** |

### 布局调整

删除右侧 3 个信息面板后，布局从 `3 列 (2+1)` 调整为：
- 分发主体：全宽单列布局，最大宽度 `max-w-4xl`
- 分发记录：从右侧小面板升级为独立区块，放在分发主体下方，使用表格展示完整记录

---

## 二、技术架构

```
┌─────────────────────────────────────────────┐
│              前端 HTML (单文件)               │
│  ┌─────────────────┐  ┌──────────────────┐  │
│  │   分发工作流     │  │   分发记录表格    │  │
│  │  (4步骤流程)     │  │  (筛选/分页/详情) │  │
│  └────────┬────────┘  └────────┬─────────┘  │
│           │                    │             │
│           ▼                    ▼             │
│     API 调用层 (fetch)                       │
└─────────────┬──────────────────┬────────────┘
              │                  │
              ▼                  ▼
┌─────────────────────────────────────────────┐
│           后端 API (Go / 现有 server)        │
│  ┌──────────────┐  ┌─────────────────────┐  │
│  │ 分发接口      │  │ 记录查询接口         │  │
│  │ POST /distribute │ GET /distributions  │  │
│  └──────┬───────┘  └─────────────────────┘  │
│         │                                    │
│         ▼                                    │
│  ┌──────────────────────────────────────┐   │
│  │  secure-distribute.sh (Bash 加密核心) │   │
│  │  - GPG 混合加密                       │   │
│  │  - SHA-256 校验                       │   │
│  │  - 会话密钥生成                       │   │
│  └──────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

---

## 三、任务分解（10 个 Agent）

### Task 1: HTML 精简与布局重构
- **负责人**: `_____`
- **状态**: `[ ] 待领取`
- **输出文件**: `frontend/public/distribute.html`

**工作内容**:
1. 基于 `分发.html` 删除右侧 3 个面板（安全状态监控、方案优势、方案对比）
2. 布局从 `grid-cols-3 (2+1)` 改为单列全宽 `max-w-4xl mx-auto`
3. 保留所有 4 个 Stage + Progress Steps + Success State + Security Modal
4. 保留现有暗色玻璃态样式（glass-panel、gradient-text 等）
5. 确保响应式适配

**验收标准**:
- 页面无报错，所有交互流程正常
- 删除的面板无残留 CSS/JS
- 移动端适配正常

---

### Task 2: 分发记录区块（前端）
- **负责人**: `_____`
- **状态**: `[ ] 待领取`
- **输出文件**: 在 Task 1 输出的 HTML 中追加

**工作内容**:
1. 在分发主体下方新增「分发记录」区块
2. 表格列：任务ID、文件名、客户名称、客户邮箱、分发时间、状态（已发送/已下载/已过期）、操作
3. 状态用彩色圆点标识（绿=已下载、黄=已发送待下载、灰=已过期）
4. 支持按状态筛选、按时间排序
5. 分页控件（前端模拟数据即可，预留 API 接口）
6. 点击行可展开详情（加密算法、SHA-256、下载 IP、下载时间）

**验收标准**:
- 表格样式与现有 glass-panel 风格一致
- 筛选和分页交互正常
- 展开详情动画流畅

---

### Task 3: secure-distribute.sh 核心加密脚本
- **负责人**: `_____`
- **状态**: `[ ] 待领取`
- **输出文件**: `scripts/secure-distribute.sh`

**工作内容**:
1. 参数解析：`--action encrypt|decrypt|verify`、`--input`、`--output`、`--recipient`、`--signer-key`、`--password-file`
2. GPG 版本检测（>= 2.2）
3. 会话密钥生成：`/dev/urandom` 生成 32 字节 base64 编码密码
4. 加密实现：`gpg --batch --yes --passphrase-file <key> --symmetric --cipher-algo AES256 --compress-algo ZLIB`
5. SHA-256 校验值计算（原始文件 + 加密文件各一份）
6. 输出 JSON 格式结果供程序调用
7. 错误处理、trap 清理临时文件
8. 支持 `--dry-run` 模式

**验收标准**:
- ShellCheck 无 error/warning
- 加密后可用 GPG 正常解密
- JSON 输出格式正确
- 大文件（>1GB）不 OOM

---

### Task 4: 客户解密指南生成器
- **负责人**: `_____`
- **状态**: `[ ] 待领取`
- **输出文件**: `scripts/generate-decrypt-guide.sh`

**工作内容**:
1. 根据加密参数自动生成 `README-DECRYPT.txt`
2. 包含 Linux/macOS 解密命令（gpg）
3. 包含 Windows 解密命令（Gpg4win）
4. 包含 SHA-256 校验步骤
5. 支持中英文双语模板
6. 模板变量替换：文件名、SHA-256 值、加密算法等

**验收标准**:
- 生成的指南客户可直接按步骤操作
- 中英文内容完整无遗漏

---

### Task 5: Go 后端 — 分发 API
- **负责人**: `_____`
- **状态**: `[ ] 待领取`
- **输出文件**: `server/internal/handler/distribute.go`

**工作内容**:
1. `POST /api/v1/distribute` — 创建分发任务
   - 接收：文件路径、客户名称、客户邮箱、分发选项
   - 调用 `secure-distribute.sh` 执行加密
   - 返回：任务 ID、加密文件路径、SHA-256、会话密钥
2. `GET /api/v1/distributions` — 查询分发记录列表
   - 支持分页：`page`、`page_size`
   - 支持筛选：`status`、`customer`
   - 支持排序：`created_at`
3. `GET /api/v1/distributions/:id` — 查询单条分发详情

**验收标准**:
- API 符合 RESTful 规范
- 错误码和错误信息清晰
- 与现有 server 路由风格一致

---

### Task 6: Go 后端 — 数据模型与存储
- **负责人**: `_____`
- **状态**: `[ ] 待领取`
- **输出文件**: `server/internal/model/distribution.go`, 迁移 SQL

**工作内容**:
1. Distribution 数据模型：
   ```
   id, task_id, file_name, file_size, encrypted_file_path,
   sha256_original, sha256_encrypted, encryption_algo,
   customer_name, customer_email, session_key_hash,
   presigned_url, url_expires_at,
   status (pending/sent/downloaded/expired),
   download_ip, download_at,
   created_at, updated_at
   ```
2. 数据库迁移脚本（SQLite 或 PostgreSQL，与现有项目一致）
3. CRUD 方法实现
4. 状态机：pending → sent → downloaded / expired

**验收标准**:
- 迁移脚本可重复执行
- CRUD 方法有单元测试
- 与现有 model 层风格一致

---

### Task 7: S3 上传与预签名 URL 模块
- **负责人**: `_____`
- **状态**: `[ ] 待领取`
- **输出文件**: `scripts/s3-upload.sh` 或 Go 模块

**工作内容**:
1. 上传加密文件到 S3 指定 bucket/prefix
2. 生成预签名下载 URL（默认 24 小时有效）
3. 支持配置有效期
4. 上传进度回调（供前端展示）
5. 支持 SCP 备选方案（无 S3 环境时）
6. 错误重试机制

**验收标准**:
- 预签名 URL 可正常下载
- 过期后 URL 失效
- 大文件上传不中断

---

### Task 8: 前端 API 对接与状态管理
- **负责人**: `_____`
- **状态**: `[ ] 待领取`
- **输出文件**: 修改 Task 1/2 的 HTML，接入真实 API

**工作内容**:
1. 将前端模拟数据替换为真实 API 调用
2. Stage 1: 调用后端获取可分发文件列表
3. Stage 2-3: 调用 `POST /api/v1/distribute`，轮询加密进度
4. Stage 4: 提交分发配置
5. 分发记录表格：调用 `GET /api/v1/distributions`
6. 错误处理与 loading 状态

**验收标准**:
- 全流程可跑通（选文件 → 加密 → 上传 → 分发 → 记录可查）
- 网络错误有友好提示
- 无 console 报错

---

### Task 9: 集成测试
- **负责人**: `_____`
- **状态**: `[ ] 待领取`
- **输出文件**: `scripts/test-distribute.sh`, `server/internal/handler/distribute_test.go`

**工作内容**:
1. Bash 脚本端到端测试：
   - 小文件加密/解密验证
   - 大文件（模拟 1GB）加密/解密验证
   - SHA-256 校验一致性
   - 错误输入处理（文件不存在、GPG 未安装等）
2. Go API 测试：
   - 创建分发任务
   - 查询分发列表（分页、筛选）
   - 状态流转测试
3. 前端冒烟测试清单

**验收标准**:
- 所有测试用例通过
- 覆盖正常流程和异常流程

---

### Task 10: 安全审计与部署文档
- **负责人**: `_____`
- **状态**: `[ ] 待领取`
- **输出文件**: `docs/secure-distribute-guide.md`

**工作内容**:
1. GPG 密钥准备指南：
   - 企业密钥对生成（RSA-4096）
   - 客户公钥导入流程
   - 密钥轮换策略
2. 安全审计清单：
   - 加密强度验证方法
   - 密钥文件权限检查
   - 传输安全检查
3. 部署步骤：
   - 依赖安装（gpg、aws-cli、pv）
   - 配置项说明
   - 服务启动命令
4. 运维手册：
   - 日常分发操作流程
   - 故障排查指南

**验收标准**:
- 新人可按文档独立完成部署
- 安全清单可逐项勾选验证

---

## 四、任务依赖关系

```
Task 1 (HTML精简) ──┐
                    ├──→ Task 8 (API对接) ──→ Task 9 (集成测试)
Task 2 (分发记录UI) ─┘         ↑
                              │
Task 3 (加密脚本) ─────────────┤
Task 4 (解密指南) ─────────────┤
Task 5 (Go API) ──────────────┤
Task 6 (数据模型) ─→ Task 5 ──┘
Task 7 (S3上传) ──────────────┘

Task 10 (文档) — 独立，可并行
```

**可并行的任务组**:
- 第一批（无依赖）: Task 1, 2, 3, 4, 6, 7, 10
- 第二批（依赖第一批）: Task 5（依赖 Task 6）
- 第三批（依赖前两批）: Task 8（依赖 Task 1, 2, 3, 5, 7）
- 第四批（最终验证）: Task 9（依赖 Task 8）

---

## 五、任务领取与回填格式

每个 Agent 领取任务后，在对应 Task 区块更新：

```
- **负责人**: `Agent-X`
- **状态**: `[x] 已完成` / `[~] 进行中` / `[!] 阻塞`
- **完成时间**: 2026-02-13 HH:MM
- **产出文件**: 实际输出的文件路径列表
- **备注**: 实施过程中的关键决策或偏差说明
```
